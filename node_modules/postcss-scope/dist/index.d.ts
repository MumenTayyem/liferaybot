import { Plugin } from "postcss";
export declare const PLUGIN_NAME = "postcss-scope";
export interface Options {
    scope: string | string[];
}
export interface Config {
    scope: string | string[];
    ignoreFile: boolean;
    ignoreRules: number[];
}
/**
 * Initialise the plugin with options
 * @param options
 */
declare function plugin(options: Options | string | string[]): Plugin;
export default plugin;
