"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.cli = void 0;
const util_1 = require("../util");
exports.cli = {
    key: 'command-line-arguments',
    description: 'From command line arguments (--key=value)',
    priority: 1000,
    get: (key, providerCache) => (0, util_1.computeIfAbsent)(providerCache, `process.args.${key}`, () => {
        const prefix = `--${key}=`;
        for (var arg of process.argv) {
            if (arg.startsWith(prefix)) {
                const value = arg.substring(prefix.length);
                return (0, util_1.unquote)(value);
            }
        }
        return undefined;
    }),
};
//# sourceMappingURL=10000-cli.js.map