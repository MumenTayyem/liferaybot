"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.applicationProfile = void 0;
const node_path_1 = __importDefault(require("node:path"));
const config_node_1 = require("../../config-node");
const util_1 = require("../util");
exports.applicationProfile = {
    key: '${CWD}/application-<profile>.json',
    description: 'From application profile property json files in ${CWD}',
    priority: 4000,
    get: (key, providerCache, commonCache) => {
        if (util_1.protectedKeys.includes(key)) {
            return;
        }
        let profile = (0, config_node_1.lookupConfig)('config.node.profiles.active');
        if (!profile) {
            return;
        }
        profile = Array.isArray(profile) ? profile[profile.length - 1] : profile;
        const cwd = (0, util_1.computeIfAbsent)(commonCache, 'cwd', () => process.cwd());
        const configPath = node_path_1.default.join(cwd, `application-${profile}.json`);
        const json = (0, util_1.computeIfAbsent)(providerCache, configPath, () => (0, util_1.readJSONFile)(configPath));
        if (json) {
            return (0, util_1.unquote)(json[key]);
        }
    },
};
//# sourceMappingURL=4000-application-profile.js.map